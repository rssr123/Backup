# =============================================================================
# QUARTZ PROPERTIES FILE - COMPLETELY COMMENTED OUT
# =============================================================================
# This file is no longer used. All Quartz configuration is now handled by:
# 1. QuartzConfig.java (programmatic configuration)
# 2. Spring Boot application.properties (database configuration via spring.datasource.*)
# 3. Spring Quartz properties (spring.quartz.properties.* in application.properties)
# =============================================================================

# Note: This file is now primarily for reference. 
# Database configuration is handled by Spring Boot via application.properties
# The QuartzConfig.java now uses Spring's DataSource instead of reading these properties

# org.quartz.scheduler.instanceId=AUTO
# org.quartz.scheduler.instanceName = MyClusteredScheduler
# thread-pool
# org.quartz.threadPool.class=org.quartz.simpl.SimpleThreadPool
# org.quartz.threadPool.threadCount=10
# org.quartz.threadPool.threadPriority=5
# org.quartz.threadPool.threadsInheritContextClassLoaderOfInitializingThread=true

# job-store
# Enable this property for RAMJobStore
#org.quartz.jobStore.class=org.quartz.simpl.RAMJobStore

# Enable these properties for a JDBCJobStore using JobStoreTX
# org.quartz.jobStore.class=org.quartz.impl.jdbcjobstore.JobStoreTX
# org.quartz.jobStore.misfireThreshold=60000
# org.quartz.jobStore.driverDelegateClass=org.quartz.impl.jdbcjobstore.StdJDBCDelegate
# Note: dataSource configuration is now handled by Spring Boot
# org.quartz.jobStore.dataSource=quartzDataSource
# org.quartz.jobStore.tablePrefix = q
# org.quartz.jobStore.useProperties=true
# org.quartz.jobStore.isClustered = true
# org.quartz.jobStore.clusterCheckinInterval = 10000
# Enable this property for JobStoreCMT
#org.quartz.jobStore.nonManagedTXDataSource=quartzDataSource

# Database configuration is now handled by Spring Boot application.properties
# The following configurations are commented out and moved to application.properties

# H2 database
# use an in-memory database & initialise Quartz using their standard SQL script
# org.quartz.dataSource.quartzDataSource.driver=com.informix.jdbc.IfxDriver
# org.quartz.dataSource.quartzDataSource.jndiURL=ssm_jndi

# PRD
# org.quartz.dataSource.quartzDataSource.URL=jdbc:informix-sqli://cmidshcl.ssm.com.my:10271/ssm_rms:INFORMIXSERVER=rmsoltp
# org.quartz.dataSource.quartzDataSource.user=rms
# org.quartz.dataSource.quartzDataSource.password=Rm$@$TG

# STG
# org.quartz.dataSource.quartzDataSource.URL=jdbc:informix-sqli://cmidsstg.ssm.com.my:10271/ssm_rms:INFORMIXSERVER=rmsoltp
# org.quartz.dataSource.quartzDataSource.user=rms
# org.quartz.dataSource.quartzDataSource.password=Rm$@$TG

# SIT
# org.quartz.dataSource.quartzDataSource.URL=jdbc:informix-sqli://10.221.29.157:4008/ssm_rms:INFORMIXSERVER=hcldev5
# org.quartz.dataSource.quartzDataSource.user=rms
# org.quartz.dataSource.quartzDataSource.password=Rm$@$TG

# LOCAL - Database configuration now uses Spring's datasource from application-local.properties
# org.quartz.dataSource.quartzDataSource.URL=jdbc:informix-sqli://20.2.0.7:9090/ssm_rms3:INFORMIXSERVER=ol_informix1410
# org.quartz.dataSource.quartzDataSource.user=informix
# org.quartz.dataSource.quartzDataSource.password=SeizeTheDay!

# org.quartz.dataSource.quartzDataSource.maxPoolSize = 5
# org.quartz.jdbc.initialize-schema=never
# org.quartz.jdbc.schema=classpath:tables_informix.sql

#org.quartz.plugin.jobInitializer.scanInterval = 0 